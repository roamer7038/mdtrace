# mdtrace

識別子でつながった Markdown 文書群をグラフとして扱う単一バイナリ `mdtrace`（Go / cobra）。
`~/.claude/CLAUDE.md` の共通ルールに加えて、この文書の決まりを適用する。

この文書は**開発時の決まりと、各情報への誘導**だけを持つ。
仕様・構造・設計判断の中身は `docs/` にあり、ここには写さない。

## 文書の地図

mdtrace 自身の文書は mdtrace で管理している。連鎖は 5 段で、リファレンスと用語集は連鎖外。

| 知りたいこと | 見る場所 |
|---|---|
| 何ができるか・使いはじめ方 | `README.md` |
| 何を達成するか・受け入れ条件・意図した対象外 | `docs/requirements.md` |
| 全体を貫く構造方針と**その理由**、却下した案 | `docs/architecture.md` |
| 外部仕様（コマンドの契約・判定基準・終了コード） | `docs/basic-design.md` |
| 内部仕様（パッケージごとの担当箇所・処理・留意点） | `docs/detailed-design.md` |
| 何をどう検査するか、機械で守れない範囲 | `docs/test-design.md` |
| 操作の手引き・フラグ一覧・つまずきやすい点 | `docs/reference.md` |
| 語の定義 | `docs/glossary.md` |
| コマンドとフラグの正典 | `mdtrace --help` |

どのパッケージを触るかは `docs/detailed-design.md` の各項目「担当箇所」から引く。

分類は 4 つ。`--help` と README・リファレンスで名前と所属を一致させる。

| 関心 | サブコマンド |
|---|---|
| 足場を作る | init, new, templates, id |
| 整合性を見る | verify |
| 文書を読む | search, list, show |
| 対応を辿る | matrix, impact, gaps, graph |

自分がこのリポジトリの文書を読むときも、利用者に案内するのと同じ順で使う。
識別子を知らないときは `mdtrace search <語>`、全体像が要るなら `mdtrace list --format tree`、
狙いが定まったら `mdtrace show <ID>`。Markdown を直接開くのは最後の手段。

## 開発コマンド

```bash
make test    # go test ./...
make lint    # gofmt -l -w . && go vet ./...
make build   # bin/mdtrace
bash scripts/regenerate-docs.sh   # 自身の文書を CLI で組み立て直す
```

## 正しさの基準

文書と実装が食い違ったとき、**どちらが正しいか**をここで決めておく。
判断の順序が無いと、直すたびに「どちらに合わせるか」を考え直すことになる。

| 順 | 正典 | 役割 |
|---|---|---|
| 1 | **`docs/`** | 要件・設計・テスト設計。挙動の約束 |
| 2 | **コード** | 唯一の実行される真実。ここが正しい |
| 3 | **`--help`（cobra の定義）** | コマンドとフラグの正典。コードから生成される |
| 4 | `README.md` / `CLAUDE.md` | 案内。上位から検証できなければならない |

**上位が変わったら下位を必ず追従させる。** 逆向きは行わない。

### 文書の役割分担

同じことを 2 か所に書かない。書くべき場所が決まっていれば、追従漏れの範囲が狭まる。

| 文書 | 書くこと | 書かないこと |
|---|---|---|
| `README.md` | 何ができるか、使いはじめ方 | 判定の細かい規則 |
| `docs/requirements.md` | 達成すること、観測可能な受け入れ条件 | 実現方法 |
| `docs/architecture.md` | 構造の**決定と根拠**、却下した案、下流への影響 | 外部仕様の細部、実装の所在 |
| `docs/basic-design.md` | 外から観測できる約束と判定の境界 | 内部の作り、フラグの網羅的な一覧 |
| `docs/detailed-design.md` | 担当箇所、内部の処理、読んで気づけない制約 | 外部仕様の再掲、決定の理由 |
| `docs/test-design.md` | 何が壊れうるか、どう検査するか | テストの一覧 |
| `docs/reference.md` | 手順、フラグの一覧と用例、直し方 | 判定基準そのもの |
| `CLAUDE.md` | 開発時の決まりと、各文書への誘導 | 利用者向けの手順、仕様や設計判断の中身、記録・履歴 |

上流ほど「なぜ」を、下流ほど「どこを」を書く。
下流が上流の文を写したら、それは重複であって整合ではない。

### 機械で守る

文書と実装の整合は、人が気づく話にしない。変更のたびに必ず取りこぼすため。

| 検査 | 見ているもの |
|---|---|
| `cmd/mdtrace/docs_test.go` | コマンド・フラグ・分類・主張された数・Go の版・設定例・JSON 例 |
| `cmd/mdtrace/diagram_test.go` | 文書中のパッケージ図の辺と実際の import |
| `pkg/trace/own_docs_test.go` | 図が指す識別子の実在と重複、横断決定の宣言と対応表の欠落の一致 |
| `pkg/verify/own_docs_test.go` | 自身の文書が全ルールを通ること、詳細設計とソースの双方向対応 |
| `pkg/scaffold/own_docs_test.go` | 識別子が `mdtrace id` で再現できること |
| `cmd/mdtrace/mdtrace_test.go` | 設定が `init` の出力そのものであること、構成の型が実際に使えること |

**数を主張したら、検査を足す。** 検査できない数は書かない。
検査の設計そのものは `docs/test-design.md` にある。

### レビューで見る（機械で守れない）

`docs/test-design.md` の「機械で守れないもの」に列挙してある。
区切りごとに `/code-review` で見る。

### 変動する値は書かない

行数・カバレッジ・バイナリサイズのように、触るたびに変わる値は文書へ書かない。
書けば必ず腐り、腐った数字は無いより悪い。必要なら求め方を書く（`make test` / `go tool cover`）。

## 設計を変えるとき

- **決着済みの設計判断は蒸し返さない**。変える前に `docs/architecture.md` の
  各項目「決定と根拠」を読む。そこにある判断を覆すなら、当時の理由が今も成り立つかを示す
- **この道具自身の約束**（実行ファイルの構え・終了コード・書き込みの規律・
  ライブラリの選定方針）はすべて `docs/architecture.md` にある。ここには写さない
- 設計方針が分かれる論点は**結論を出さず**、選択肢・利害得失・影響範囲を示して判断を仰ぐ

## ライブラリの採用

判断の基準は `docs/architecture.md` の「外部ライブラリの選定方針」にある。
実際に取り込んで動かしてから判断すること、この構成の文書で試した結果を根拠にすることを守る。

## コメントと文書

- コメントは**事実と意図**を書く。変更の経緯・履歴・指摘への言及は書かない
  - 悪い例: 「以前は X だったが Y に変更」「レビュー指摘により修正」
  - 良い例: 「Y を使う。X は Z に依存しバイナリが 1MB 増えるため」
- godoc 規約に従う（`// 名前 は …`）。エクスポートした要素にはすべて付ける
- 「なぜそうしたか」が非自明な箇所にだけ理由を書く。自明な説明は足さない
- **作業ログをリポジトリに残さない**。文書もコメントも「現在どうなっているか」で書く
- 文書の新規作成・変更は依頼されたときに行う

## 自身の文書との整合

`docs/` は mdtrace 自身の要件・設計・テスト設計で、テストが実装との対応を検査している。

- **挙動の約束を変えたら、必ず該当する文書の記述を追従させる**
- 追従したら `bash scripts/regenerate-docs.sh` を実行する
- 設定と識別子の採番は CLI が生成する。手で書かない
  - 用語集の**見出しと本文は人が書く**（他の文書タイプと同じ）。採番だけが CLI の仕事
  - 階層識別子（`R-1.2` の形の細目）だけは人が振る。CLI が払い出すのは連番のみ
- `testdata/` には検証用の固定データ以外を置かない。生成スクリプトなどは `scripts/` へ
- 見出しの階層に注意する。H2 が主レベル（＝必須セクションの検査単位）なので、
  説明や図を足すときは H3 以下を使う。H3 に識別子を付けると細目として扱われる
- **項目を足すときは文書の末尾へ足す**。`mdtrace id` は文書順に採番するので、
  途中に挿すと番号順と文書順がずれ、再現テストが落ちる。
  順序を変えたいときは識別子を剥がして振り直す。
  細目は自動採番の対象外なので、途中に挿してよい
- **例に実際の識別子の書式を使わない**。コードブロックの中に `## REQ-1:` と書くと、
  採番を振り直したときにそこだけ取り残される。例には使っていない書式（`R-` など）を使う
- 人が読んで理解できる情報を含める。構造は Mermaid で図示し、読み方（各節に何を書くか）を添える

## テスト

- 修正には再現テストを添える。テストが落ちることを確認してから直す
- 期待値は独立に算出できるものは算出する（固定値の埋め込みに頼らない）
- 文書とコードの対応はテストで固定する
- **構成の型はすべて動くことをテストで固定する**（型を足したら検査対象も自動で増える）。
  特定の進め方に寄らないという主張は、組み込みの型ではなく
  段数と文書タイプ名が違う構成を通すことで裏づける

## 進め方

- 複雑な作業は計画を提示し、承認後に着手する
- 区切りで `/code-review` を受ける。実行中はファイルを変更しない（並行編集はレビューを壊す）
- 指摘は**再現を確認してから**直す。助言どおりに直して解決しない場合は、原因から捉え直す
- 主張は実測で裏づける（バイナリサイズ・処理速度・カバレッジ・行数）
